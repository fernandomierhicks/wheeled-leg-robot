




---Fernandos note dont change below


Limit siwthc instead of hardstops? Move entire leg forward to better cpature CG. then limit switch at the current hardstop. Longer lever from coupler to limit switch. 
-Build in adjustability (slotted bracket) so you can tune the exact trigger angle post-assembly and match left/right legs — any asymmetry between the two legs' home offsets will bias the balance controller.
-Use NC (normally closed) wiring if the switch supports it, so a broken wire or connector pop reads as "triggered/fault" rather than silently reading as "never triggered" — fails safe.
-Overtravel margin: pick a switch with enough spec'd overtravel to survive the motor's stopping latency (time from trigger to actual motor stop) at your homing speed, so you're not relying on zero-latency stop.


WIFI- test with actual GUI


YAW-Flip control signal and TX? its backwards in gains and in remote. 

Do export of gains. 

------------------------------------------------------firmware
--------------------------NON-CRITICAL
Add screen personolity selector to radio. 
Parameter or menu options via remote?



----------------------------------------------------------CAD
Todo list in CAD folder


-------------------------------------------------------Jump
For jump height, box mass is much less sensitive than wheel mass. Wheel mass direct penalty, box mass might help as long as motor can accelerate it fast enough



---------------------------------------------------------SIMULATION
-REtune robot with AK45 as wheel motor?

-Flip?
Want me to build this as a new scenario (S11)? The sequence would be:
Settle + drive forward at max speed for ~1.5s
Brake hard (or just jump without braking — the hip extension alone might be enough)
Jump
Retract legs immediately after liftoff
Land (somehow...)



-----------------------------------------------------------CUBEMARS
LEG MOTOR
-Add CAN interface Via arduino

---------------------------------------------------------ODRIVE

-implement the potential odrive firmware improvements in components\characterization\odrive\forkedFirmware
Do 30min anticogging run or longer for both motors
-Fix encoder parity issue in README.MD



--MOtor
-Spinout detection
-Hard bus current limits
-Leak current limit 
-Live Power Monitoring Torque estimation

--CAN into ODRIVE to check latencies. 



